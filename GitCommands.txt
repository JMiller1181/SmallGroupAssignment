git checkout - b BranchName
Creates a new branch titled BranchName
and switches over to it.

git status
gives status of files in branch 

git add
Selects and adds files so they are ready to be commited

git pull
pulls the latest version of the repo
